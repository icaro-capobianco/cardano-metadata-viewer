{
  "name": "cardano-metadata-viewer",
  "version": "1.0.0",
  "author": "Ícaro C. Capobianco",
  "license": "MIT",
  "scripts": {
    "dev": "preact watch --src src/frontend --sw false --babelConfig config/.babelrc --template src/frontend/template.html --config config/preact.config.js --no-prerender --host localhost",
    "build": "preact build --src src --dest build --sw false --babelConfig config/.babelrc --template src/template.html --config config/preact.config.js --no-prerender"
  },
  "dependencies": {
    "@chakra-ui/react": "^1.3.3",
    "@emotion/react": "^11.1.5",
    "@emotion/styled": "^11.1.5",
    "preact": "^10.5.12",
    "preact-router": "^3.2.1",
    "react-icons": "^4.2.0"
  },
  "devDependencies": {
    "preact-cli": "^3.0.5",
    "typescript": "^4.0.5"
  }
}
